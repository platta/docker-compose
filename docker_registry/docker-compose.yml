version: "3"

services:
  registry:
    image: registry:latest
    container_name: docker_registry
    ports:
      - 5000:5000
    volumes:
      - data:/var/lib/registry

volumes:
  data:
    driver: local
